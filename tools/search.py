import requests

def web_search(query: str) -> str:
    """
    Simple web search tool (placeholder).
    In production, you’d integrate Bing, Google, or another API.
    """
    return f"[Search results for: {query}]"
